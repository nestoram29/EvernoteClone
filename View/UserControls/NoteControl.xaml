<UserControl x:Class="EvernoteClone.View.UserControls.NoteControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EvernoteClone.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="600">
    <StackPanel>
        <TextBlock Text="{Binding Title}" Foreground="Black" Margin="15, 15, 15, 5" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" />
        <TextBlock Text="{Binding UpdatedAt}" Foreground="DodgerBlue" Margin="15 5 15 0" FontSize="16" VerticalAlignment="Center" />
    </StackPanel>
</UserControl>
